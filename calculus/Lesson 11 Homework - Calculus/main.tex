\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage[thinc]{esdiff}
\usepackage{amsmath}
\title{Lesson 11 Homework - Calculus}
\author{William Wang}
\date{September 2026}

\begin{document}

\maketitle

\section{}
We want to find the first and second order derivatives of 
\[r = \frac{(\theta - 1)(\theta^2+\theta+1)}{\theta^3}\]
We can see that the numerator is the factorization of difference of cubes, hence we have 
\[r = \frac{\theta^3 - 1}{\theta} = 1+ \frac{1}{\theta^3} = 1+ \theta^{-3}\]
Then differentiating using the power rule, we have
\[\diff{r}{t} = 1 -3\theta^{-4} = 1 - \frac{3}{\theta^4}\]
Then the second order derivative is 
\[\diff[2]{r}{t} = \frac{d}{dt} (1-3\theta^{-4}) = 1 +\frac{12}{\theta^5}\]

\section{}
Since it passes through \((1,2)\), we have 
\[a+b+c = 2 \]
And since it passes through \((0,0)\), 
\[c = 0\]
Since we know that \(y = ax^2 +bx+c\) is tangent to \(y=x\) at the origin, we can find the derivative of \(y\)
\[\diff{y}{x} = 2ax + b\]
when we substitute in \((0,0)\) has to have equal \(1\) since it has the same slope as \(y=x\). Hence,
\[y^\prime(0) =b = 1\]
Then plugging in \((1,2)\), with \(b= 1, c = 0\), we get 
\[a + 1 + 0 = 2 \implies a = 1\]
Hence
\[y = x^2+x\]

\section{}
\subsection{}
We want to find the derivative of 
\[y = 3\cosh x + 7^x\]
which we can write as from the definition of \(\cosh\)
\[y = 3\cdot\frac{e^x+e^{-x}}{2} + 7^x\]
we can differentiate each term and then add them together. 
\[\diff{}{x} \frac{3}{2}(e^x+e^{-x}) = \frac{3}{2} (e^x - e^{-x}) =  3\sinh x\]
\[\diff{}{x} 7^x = 7^x \ln(7)\]
Hence
\[\diff{y}{x} = 3\sinh x + 7^x\ln(7)\]
\subsection{}
We want to find the derivative of 
\[f(x) = \sinh x + \cosh x - 2x\]
By deriving each term
\[\diff{}{x} \sinh x = \cosh x\]
\[\diff{}{x} \cosh x = \sinh x\]
\[\diff{}{x} 2x = 2\]
Hence,
\[\diff{f}{x} = \sinh x + \cosh x + 2\]
\subsection{}
We want to find the derivative of 
\[g(u) = u \cosh^2(u) + \pi^u - u\sinh^2(u)\]
We can first use the identity, that \(\cosh^2 x - \sinh^2 x =1\), to simplify
\[g(x) = u(\cosh^2 u - \sinh^2 u) + \pi^u = u + \pi^u\]
Then we have 
\[\diff{g}{u} = \diff{}{u} u + \pi^u = 1 + \pi^u \ln(\pi)\]


\end{document}
